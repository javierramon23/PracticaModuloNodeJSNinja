/**
 * Este fichero define una RUTA BASE donde se almacenan la IMAGENES de los distintos OBJETOS.
 */
// Para EJECUTAR el código JS en 'modo estricto', que es menos 'flexible' con los errores de sintaxis de JS.
'use strict';

/**
 * Se EXPORTA el objeto JSON para poder utilizarlo en otros ficheros.
 */
module.exports = {
  anuncios: {
    imagesURLBasePath: '/images/anuncios/'
  }
};
